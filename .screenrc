# Wed Nov 21 21:19:39 EST 2012
# From Sven Guckes's screenrc here:
# http://www.guckes.net/Setup/screenrc
# 
# bufferfile:   The file to use for commands
#               "readbuf" ('<') and  "writebuf" ('>'):
bufferfile    $HOME/.screen_exchange
defscrollback 30000 # Use a 30000-line scrollback buffer

# Jeff's screenrc file
#
# An alternative hardstatus to display a bar at the bottom listing the
# windownames and highlighting the current windowname in blue. (This is only
# enabled if there is no hardstatus setting for your terminal)
hardstatus on
hardstatus alwayslastline
hardstatus string "%{.4W}%-w%{.rW}%n %t%{-}%+w %=%{..G} %H %{..Y} %m/%d %C%a "

# shelltitle ''

# Start a bunch of windows by default
screen -t _bash 0
screen -t _console 2 bundle exec rails console
screen -t _server 3 bundle exec rails server
screen -t _psql 4 # env -i EDITOR='vim -c "se nowrap nostartofline syntax=sql mouse="' PAGER='vim - -c "se nowrap nostartofline"' psql -d ff_dev -U postgres
